/** @type {import('next').NextConfig} */

const nextConfig = {
  reactStrictMode: true,
  swcMinify: true,

  // images: {
  //   domains: ["pixabay.com", "cdn.pixabay.com ", "google.com"],
  // },
};

module.exports = nextConfig;
